# Scr33nX — Python dependencies
# Install with:  pip install -r requirements.txt
# Then (Stripchat browser fallback only):  playwright install chromium

requests>=2.31.0                            # HTTP: site APIs + local HLS relay (cb_relay.py)
websocket-client>=1.7.0                     # MyFreeCams FCS protocol resolver (mfc.py)
winotify>=1.1.0; sys_platform == "win32"    # Windows toast notifications (notifier.py)
playwright>=1.50.0                          # Stripchat fallback recorder (stripchat_live.py)
tdjson>=1.8.56                              # Telegram upload pipeline (pipeline_worker.py)
